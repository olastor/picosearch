import { buildSearchIndex, querySearchIndex } from '../src/index'

describe('TinySearch', () => {
  describe('Options', () => {})
  describe('buildSearchIndex()', () => {})
  test('empty string should result in zero', () => {
    expect(0).toBe(0);
  });
});
