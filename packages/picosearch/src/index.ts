export type {
  Analyzer,
  Document,
  SearchResult,
  SearchResultWithDoc,
  SerializedInstance,
  Tokenizer,
  SyncResult,
  FetchMetadata,
  Patch,
} from './types';

export type {
  Options,
  QueryOptions,
  SearchIndex,
  SyncOptions,
} from './schemas';

export { Picosearch, defaultTokenizer, defaultAnalyzer } from './picosearch';
